# Local development environment variables
# Copy this file to .env and update values as needed

# Secret key for session management (change in production!)
SECRET_KEY=your-secret-key-here

# Database configuration
# Local: SQLite database stored in instance/tasks.db (persists across restarts)
# Production: PostgreSQL URL will be automatically set by Render
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Admin account (DO NOT CHANGE - hardcoded in server.py)
# Email: kalanadenuz@gmail.com
# Password: 12345678

# For production deployment on Render:
# 1. DATABASE_URL will be automatically set by Render PostgreSQL
# 2. Set SECRET_KEY in Render environment variables (use strong random value)
# 3. Admin account will be created automatically on first run
